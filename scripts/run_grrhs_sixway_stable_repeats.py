from __future__ import annotations


def main() -> None:
    print(
        "This legacy Exp3 stable-repeat script has been retired. "
        "Use `python -m simulation_second.src.run_blueprint run-benchmark` "
        "with explicit `--settings`, `--methods`, and `--repeats` options."
    )


if __name__ == "__main__":
    main()
