from __future__ import annotations


def main() -> None:
    print(
        "This legacy random-coefficient probe depended on the retired Exp3 route. "
        "Use `python -m simulation_second.src.run_blueprint sample-setting` "
        "or `run-benchmark` on the active simulation_second suite instead."
    )


if __name__ == "__main__":
    main()
