name: "toy_weak_signals_main"
output_dir: "outputs/sweeps/toy_weak_signals_main"

common_config_files:
  - "../experiments/toy_weak_signals.yaml"

common_overrides:
  experiments:
    repeats: 1

variations:
  - name: "grrhs"
    config_files:
      - "../methods/grrhs_regression.yaml"

  - name: "rhs"
    config_files:
      - "../methods/regularized_horseshoe.yaml"
    overrides:
      model:
        backend: "numpyro"

