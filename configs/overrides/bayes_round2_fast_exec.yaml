experiments:
  bayesian_fairness:
    disable_budget_retry: false
    sampling_budget:
      burn_in: 1200
      kept_draws: 1200
      thinning: 1
      num_chains: 4

  posterior_validation:
    seed_stability:
      num_restarts: 1

inference:
  nuts:
    target_accept_prob: 0.99
    max_tree_depth: 12
