"""Compatibility package only.

Active experiments live in ``simulation_second``, ``Simulation_highdimension``,
``simulation_mechanism``, and ``real_data_experiment``. This namespace remains
only so older imports can reach the shared Bayesian kernel while the retired
Exp1-Exp5 orchestration route stays inactive.
"""
